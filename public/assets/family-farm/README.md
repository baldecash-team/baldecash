# family-farm assets (DEMO — BAL-2522)

Placeholder directory. The real mascot image has not been provided yet.

Expected file: `mascota.png` (referenced from `FamilyFarmOverlayGate` in
`src/app/prototipos/0.6/[landing]/layout.tsx` via `// TODO: replace with real
URL when provided`).

Drop the final PNG here with that exact filename once it's available. Do not
fabricate binary image data in the meantime — the component references the
path defensively (broken `<img>` degrades gracefully, no build-time or
runtime crash).
